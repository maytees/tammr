let numbers = [1, 2, 3, 4, 5]
fprintln("First number: {}", numbers[0])
fprintln("Last number: {}", last(numbers))

numbers = push(numbers, "test")
fprintln("After push: {}", numbers)

numbers = pop(numbers)
fprintln("After pop: {}", numbers)
