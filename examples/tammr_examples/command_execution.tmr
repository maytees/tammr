// Executing a system command
let str result = exec("echo Hello from Tammr")
println("Command output: ${result}")

// Using command output in Tammr
let str files = exec("ls -l")
println("Files in current directory:")
println(files)
