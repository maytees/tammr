let str str1 = "Hello"
let str str2 = "World"

// Concatenation
let str combined = str1 + " " + str2
println(combined)

// Interpolation
let str name = "Tammr"
println("Welcome to ${name} language!")

// String methods
println(combined.toUpper())
println(combined.toLower())
println(combined.length())
println(combined.contains("lo"))

let str csv = "apple,banana,cherry"
let str fruits = csv.split(",")
foreach fruit in fruits do
    println(fruit)
end
